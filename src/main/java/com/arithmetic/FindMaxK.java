package com.arithmetic;/** * @program: AutoTestDemo * @description: 查找大量无序元素中最大的K个数 * @author: gonghuihui * @create: 2018-09-19 11:06 **/public class FindMaxK {    /**     * @Description: 将数组a[s]...a[t]中的元素用一个元素划开，保存中a[k]中     * @Param:     * @return: void     * @Author: gonghuihui     * @Date: 2018/9/19     */    public static void findMaxKA(int a[], int s, int t, int k[]){        int i, j, x;        x = a[s];        i = s;        j = t;        do {            while ((a[j] < x)) j--;   // 从右向左扫描，如果比划分元素小，则不动            if (i<j) a[i++] = a[j];   // 大元素向左边移动            while ((a[i] >= x) && i<j) i++;    // 从左向右扫描，如果比划分元素大，则不动            if (i<j) a[j--] = a[i];   // 小元素向右边移动        }        while (i<j);   // 直到i与j相等        a[i] = x;    }}