package com.demo.classes;import java.io.IOException;import java.io.PipedOutputStream;/** * @program: AutoTestDemo * @description: 发送者线程 * @author: gonghuihui * @create: 2018-10-12 17:46 **/public class Sender extends Thread {    // 管道输出流对象    private PipedOutputStream out = new PipedOutputStream();    // 获取"管道输出对象"    public PipedOutputStream getOutputStream() {        return out;    }    @Override    public void run() {        writeShortMessage();//        writeLongMessage();    }    private void writeShortMessage() {        String strInfo1 = "This is a short message";        try {            out.write(strInfo1.getBytes());            out.close();        } catch (IOException e) {            e.printStackTrace();        }    }    private void writeLongMessage() {        StringBuilder sb = new StringBuilder();        for (int i = 0; i < 102; i++)            sb.append("0123456789");        sb.append("abcdefghijklmnopqrstuvwsyz");        String strInfo2 = sb.toString();        try {            out.write(strInfo2.getBytes());            out.close();        } catch (IOException e) {            e.printStackTrace();        }    }}