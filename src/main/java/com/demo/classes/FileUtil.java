package com.demo.classes;import java.io.File;import java.io.FileFilter;import java.util.AbstractQueue;/** * @program: AutoTestDemo * @description: FileFilter学习 * @author: gonghuihui * @create: 2018-10-11 19:20 **/public class FileUtil implements FileFilter {    @Override    public boolean accept(File pathname) {//        if (pathname.isDirectory())   // 如果是文件夹直接返回true//            return true;//        else {            String name = pathname.getName();            if (name.endsWith(".txt"))                return true;            else                return false;//        }    }    public void getAllFilePath(String dir) {        File directory = new File(dir);        if (!directory.isDirectory()) {            System.out.println("No directory provided");            return;        }        File[] files = directory.listFiles(new FileUtil());        for (File f : files) {            System.out.println(f.getName());        }    }}